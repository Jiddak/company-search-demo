spring:
  application:
    name: company-search-demo
  datasource:
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: test
    password: test

true-proxy:
  base-url: 'http://test'
  cache:
    enabled: true
    cache-size: 100
    cache-duration-millis: 36000
